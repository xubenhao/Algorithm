#define ALGORITHMLIB __declspec(dllexport)
#include "ZeroOnePack.h"
int CalculateMaxValue1(
	int* nValueArr_, 
	int* nWeightArr_, 
	int nStartPos_, 
	int nEndPos_, 
	int nCapcity_)
{
	// 略
	// 集合中选取一个元素 + 剩余元素的最优方案
	// 从n个上述形式中，选取n个结果中最优的一个作为最终的结果
	return 0;
}

int CalculateMaxValue2(
	int* nValueArr_, 
	int* nWeightArr_, 
	int nStartPos_, 
	int nEndPos_, 
	int nCapcity_, 
	int* pResult_, 
	int nMaxNum_)
{
	// 略
	// 集合中选取一个元素 + 剩余元素的最优方案
	// 从n个上述形式中，选取n个结果中最优的一个作为最终的结果
	// 加入查表支持
	return 0;
}
